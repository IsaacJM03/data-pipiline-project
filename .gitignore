__pycache__/
*.py[cod]
# Runtime pipeline artifacts
# outputs/exports and outputs/reports are intentionally tracked

# Optional local raw input dumps
# Keep directory structure with .gitkeep only
data/raw/*.csv
data/raw/*.tsv
data/raw/*.txt
*.zip
*.claude/
.vscode/settings.json
.DS_Store
*.log
outputs/cache/

